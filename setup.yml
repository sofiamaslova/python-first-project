---
- hosts: all
  become: yes # This means that all tasks will be executed with sudo 
  tasks:
   - name: Install Python 3
     apt:
           name: python3
           state: present
           update_cache: yes

